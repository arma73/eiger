{
  "name": "task-first",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tasks/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/tasks",
        "main": "tasks/src/index.ts",
        "tsConfig": "tasks/tsconfig.lib.json",
        "assets": ["tasks/*.md"]
      }
    }
  }
}
